export enum TransferType {
  SEND = 'send',
  SWAP = 'swap',
  UNSWAP = 'unswap',
  RECEIVE = 'receive',
}

export enum NetworkType {
  ETHEREUM = 'ethereum',
  BSC = 'binance',
  ARBITRUM = 'arbitrum',
  POLYGON = 'polygon',
  AVALANCHE = 'avalanche',
}
