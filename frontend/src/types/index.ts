export enum TransferType {
  SEND = 'send',
  SWAP = 'swap',
  UNSWAP = 'unswap',
  RECEIVE = 'receive',
}

export enum NetworkType {
  Polygon = 'polygon',
  Ethereum = 'ethereum',
  BSC = 'binance',
  ARBITRUM = 'arbitrum',
}
